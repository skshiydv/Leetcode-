    public int removeDuplicates(int[] nums) {
        int i = 0 ,s=0;
        for (i=0; i<nums.length-1; i++){
                 if (nums[i]!=nums[i+1] ){
                     nums[s++]=nums[i];
             }

        }
        nums[s]=nums[nums.length-1];
    return s;
    }
